{
  "version": 1,
  "build": [
    {
      "src": "@server/src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "@server/(.*)",
      "dest": "@server/src/main.ts",
      "method": ["GET", "POST", "PUT", "DELETE", "PATCH"]
    }
  ]
}
