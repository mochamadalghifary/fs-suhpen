{
  "version": 1,
  "builds": [
    {
      "src": "@server/src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "@server/(.*)",
      "dest": "@server/src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
    }
  ]
}
